<%= form_with(model: post, local: true) do |form| %>
    <%= form.label :title %>
    <%= form.text_field :title, placeholder: 'Your title here', equired: true %>

    <%= form.label :content %>
    <%= form.text_area :content, placeholder: 'Your content her' %>

    <%= form.submit %>

<%end%>

