<nav id="navbar" class="navbar navbar-expand navbar-dark bg-dark">
  <span class="navbar-brand">Nathan's Forum</span>
  <button type="button" class = "navbar-toggler" data-toggle="collapse" data-target="#navbarNavAltMarkup">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div id="navbarNavAltMarkup" class="collapse navbar-collapse">
    <ul class="navbar-nav">
      <li class="nav-item " ><%= link_to t('.home'), posts_path%></li>
      <li class="nav-item" ><%= link_to t('.contact'), contact_path%></li>
      <%if user_signed_in? %>
        <li class="nav-item" ><%= link_to t('.my_posts'), myposts_path%></li>
        <li class="nav-item" ><%= link_to t('.create'), new_post_path%></li>
        <li class="nav-item" ><%= link_to t('.sign_out'), destroy_user_session_path, method: :delete%></li>
      <% else %>
        <li class="nav-item" ><%= link_to t('.sign_in'), new_user_session_path%></li>
        <li class="nav-item" ><%= link_to t('.sign_up'), new_user_registration_path%></li>
      <% end %>
    </ul>
  </div>
  <%if user_signed_in? %>
    <ul id = "list" class="navbar-nav ml-auto">
      <li class="nav-item">You are logged in as <%=current_user.email%></li>
    </ul>
  <% else %>
    <ul id = "list" class="navbar-nav ml-auto">
      <li class="nav-item">You are not logged in</li>
    </ul>
  <%end %>
</nav>